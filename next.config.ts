import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["jmvacxdzxlfcrwibtkbb.supabase.co"], // 🔥 Sostituisci con il tuo dominio Supabase!
  },
};

export default nextConfig;
